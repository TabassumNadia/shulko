# secrets — never commit
.env
.env.backup*

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# data + db
data/chroma/
*.db
*.sqlite3

# os / editor
.DS_Store
.vscode/
.idea/

# a stale partial copy of this project that an earlier unzip left behind:
# 150 MB of virtualenv and its own .env, no source. Safe to delete.
/shulko/

# duplicate copies of data/samples/ under a confusing name, left by an
# earlier session. The samples themselves are tracked in data/samples/.
Claude outputs/

# screenshots are regenerated by docs/capture_screenshots.py, but they are
# committed so the README renders on GitHub -- only the scratch output of a
# failed capture is ignored.
docs/screenshots/*.tmp.png
